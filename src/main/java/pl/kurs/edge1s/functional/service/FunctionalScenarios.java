package pl.kurs.edge1s.functional.service;

import java.util.List;

public class FunctionalScenarios {

    /**
     * Gets greatest even number from argument list.
     *
     * @param numbers
     * @return greatest even number or throws runtime exception when there are
     * no even numbers
     */
    public int greatestEvenNumber(List<Integer> numbers) {
        return 0;
    }

    /**
     * Gets greatest odd number from argument list.
     *
     * @param numbers
     * @return greatest odd number or throws runtime exception when there are no
     * odd numbers
     */
    public int greatestOddNumber(List<Integer> numbers) {
        return 0;
    }

    // Try to make one method called greatestNumber which will take two
    // arguments:
    // - one is list of numbers of course
    // - second is function which will decide if we want to find odd or even
    // numbers

    /**
     * @param words words lists, WARNING: may include null words inside or can be
     *              null itself.
     * @param n     number of greatest words we would like to find
     * @return list of top n greatest words or runtime exception if for any
     * reason this is not possible
     */
    public List<String> topN(List<String> words, int n) {
        return null;
    }

}
